@model IEnumerable<RawTorpedo.Models.Collection>

@{
    ViewData["Title"] = "Index";
}

<h2>My Games</h2>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <tr>
        <th>
            Collection
        </th>
        @*<th></th>*@
    </tr>

    @foreach (var item in Model.Where(x => x.CollectionType == RawTorpedo.Domain.Enums.CollectionType.Collection))
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Game.GameName)
            </td>
            @*<td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                </td>*@
        </tr>
    }
</table>

<table class="table">
    <tr>
        <th>
            Wishlist
        </th>
        @*<th></th>*@
    </tr>
    @foreach (var item in Model.Where(x => x.CollectionType == RawTorpedo.Domain.Enums.CollectionType.Wishlist))
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Game.GameName)
            </td>
            @*<td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                </td>*@
        </tr>
    }
</table>
